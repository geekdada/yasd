import tw from 'twin.macro'

const PageLayout = tw.main`relative h-full overflow-y-scroll bg-background antialiased max-w-4xl mx-auto border-l border-r border-gray-200 dark:border-gray-800`

export default PageLayout
